__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# never commit secrets
*.env
config.yml
config.yaml
nomwatch.conf
credentials.json
token.json
*.pem
*.key

.DS_Store

# nomwatch ui runtime artifacts (startnom/quitnom/nomstatus skills)
.nomwatch-ui.pid
.nomwatch-ui.port
.nomwatch-ui.log

# Obsidian (this repo doubles as a vault - don't commit its app config/workspace state)
.obsidian/

# Claude Code local skill config (personal tooling, not part of the project)
.claude/
.nomwatch-run.log

# MediaMTX drops auto-generated TLS certs into its CWD; NomWatch now pins
# that to ~/.config/nomwatch, but this keeps older versions from polluting the repo
auto.crt

nomwatch-*/
